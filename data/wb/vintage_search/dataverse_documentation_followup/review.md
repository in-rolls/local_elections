The four requested documentation files remain unavailable through ordinary unauthenticated downloads. Each documented `GET /api/access/datafile/{id}` returned HTTP 400 and identified guestbook 269 as a prerequisite. No guestbook response, personal details, credentials, alternate download bypass, or access request was submitted. The public [Dataverse API guide](https://guides.dataverse.org/en/latest/api/dataaccess.html#basic-file-access) describes this requirement. Its fetched HTML is retained locally.

| Requested file | Dataverse file ID | Result |
|---|---:|---|
| Readme.pdf | 13986331 | HTTP 400: guestbook 269 required |
| codebook_pradhan.pdf | 13986362 | HTTP 400: guestbook 269 required |
| Clean_Election_data_gp.do | 13986343 | HTTP 400: guestbook 269 required |
| CleanPradhanAll.do | 13986336 | HTTP 400: guestbook 269 required |

The public guestbook metadata identifies J-PAL Guestbook (v2). It requires a name and custom answers about intended use, position and institution country. Full metadata is retained in `responses/guestbook_metadata_00.body`; `summary.json` lists its required custom questions. No answers were supplied. The files' unrestricted flags in dataset metadata do not eliminate the guestbook requirement.

The [Northwestern author page](https://faculty.wcas.northwestern.edu/lab823/) supplied two independently public appendices, both acquired successfully: [data appendix](https://faculty.wcas.northwestern.edu/lab823/powerfulwomen_data_appendix.pdf), five pages, and [table appendix](https://faculty.wcas.northwestern.edu/lab823/PowerfulWomen_Tables_Webappendix.pdf), six pages. Full response bodies and page-level native text are retained under `responses/` and `extracted/`. These are publication appendices, not copies of the four requested files.

The data appendix p1 explains randomized leader gender in speeches/vignettes. Table appendix p3 (Table A.3) and p5 (Table A.5) define their Female Pradhan variable using the speaker or depicted leader in those experiments. Thus that variable is a stimulus assignment and does not validate a coding rule for elected officeholders. Table appendix p4 (Table A.4) defines ever-reserved using the 1998 and 2003 reservation histories. Neither appendix supplies the raw gender1998/gender2003/gender2008 code mapping, a named-GP identity bridge, or data file IDs. No elected-head gender labels were inferred from these appendices.

The [MIT author publication index](https://economics.mit.edu/people/faculty/esther-duflo/published-papers) links the aspirations paper's DATA to Harvard Dataverse handle `1902.1/17154`, not an independent host. That linked landing-page request returned HTTP 202 with an empty body. The MIT bias-paper publication page was accessible and linked a paper PDF, without a separate replication package. The MIT aspirations publication page returned HTTP 403. NBER's public working-paper page was accessible but did not yield the four requested files. Searches for exact filenames and author/replication combinations did not surface an independently public copy in this bounded review; this is not proof that no mirror exists.

`target_and_related_file_ids.json` preserves the four documentation IDs and related election/Pradhan dataset IDs from the existing O3UKFO metadata snapshot. Dataset ID is 65898; DOI is `10.7910/DVN/O3UKFO`. The snapshot's original path and SHA256 are recorded explicitly: it is retained evidence, not a successful metadata refresh. A new request to `/api/datasets/65898` returned HTTP 403. Relevant raw/clean files include `pradhan.tab` 13986376, `pradhan_clean.tab` 13986321, `election_pradhan_2008.tab` 13986308 and `election_pradhan_alldist.tab` 13986379. These identifiers alone establish no gender semantics or cross-file joins.

`fetch_log.jsonl` retains request URLs, UTC start/finish times, statuses or transport errors, redirects, response headers, body paths, byte lengths and SHA256 values. `http_response_inventory.csv` provides a flattened response ledger. Eight initial sandbox attempts failed DNS before an HTTP response; these are recorded as transport failures, not server denials. Automatic approval review timed out once; the allowed retry succeeded. No failed authorization decision was bypassed.

Local validation recomputed the length and SHA256 of every retained response, checked the four target IDs against the metadata snapshot, and confirmed native text extraction for all 11 acquired appendix pages. No existing audits, parsers, or derived datasets were changed. The raw elected-Pradhan gender coding remains unresolved pending legitimately obtained documentation.

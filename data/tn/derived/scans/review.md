The selected extraction covers all 114 PDF pages of Tamil Nadu Gazette No.401 (1996), plus PDF pages 1–18 of No.636 (2001). The other 59 pages of No.636 are explicitly outside this office-section extraction: a separate ward-reservation amendment begins on PDF page 19. Selection is source scope, not a claim that all 132 pages contain village-president records.

The 1996 source includes village presidents, union chairs, district chairs and backmatter. The 2001 selection includes mixed-office amendments, not a new complete statewide village-president roster. `reviewed_scope.json` preserves the bounded page-level roles from the prior visual source audit, with the audit and review-image hashes. Uninspected intermediate pages are not assigned a tier from adjacent pages. No elected-person gender is inferred.

`pilot_review.json` records direct source-image checks on 1996 PDF pages 1/3 and 2001 pages 1/2. Printed body text on these pages is English, supporting eng OCR for this extraction. Tamil is selectable through `--language eng+tam`; it was not silently applied to English body text. The pilot is developmental legibility review, not a held-out accuracy evaluation. The 1996 roster has faint names and badly segmented serials; 2001 page 2 has clearer cells but ruled borders contaminate serials. Original images and raw text/TSV remain authoritative evidence for review, not validated data fields.

Resume command: `.venv/bin/python -m local_reservations.states.tn.extract_scans --workers 4`. Use `--pilot` for the four pilot pages. Each selected page directory contains `latest.json` pointing to an immutable `attempts/<id>/` directory. The attempt preserves the full 300-dpi PNG, Tesseract text/TSV, diagnostics and metadata. Cached success requires identical source/page/engine/settings identity and matching hashes and byte lengths for every artifact. Changed settings, corrupt files and failed attempts trigger another extraction; previous attempts remain on disk.

TSV coordinates are pixels in the stored page image, with origin at its upper left. Metadata records image dimensions and the source PDF page bounding box; no row grouping or PDF-coordinate projection is implied. Full Tesseract and Poppler versions, active traineddata paths/SHA256, OCR language/OEM/PSM/DPI and OMP_THREAD_LIMIT=1 are retained. Source hashes are checked against both the acquisition manifest and pinned expected hashes before extraction. `sources.json`, `engine.json`, `progress.jsonl`, `page_audit.json/csv` and `summary.json` provide document/run/page provenance and coverage. Every selected page retains semantic status not_parsed.

Ready regions for subsequent source-validated parsing include the 2001 Devakottai replacement on PDF pages 2–3 and the other explicitly identified GP amendments; these must preserve substitution/insertion operations and be linked to the 1996 base where required. The 1996 two-column GP roster needs separate column/row geometry and source review of names and serials. Union-chair and district-chair regions must stay separate. OCR presence and word counts are not reservation-record counts or accuracy estimates.

After the core 132 pages completed, an optional two-page 2006 No.228 pilot was saved separately in `pilot_2006/`. Both pages extracted, but name and category OCR errors remain (including SC misread as SE). The other 507 PDF pages were not attempted; no complete 2006 extraction or amendment integration is claimed.
